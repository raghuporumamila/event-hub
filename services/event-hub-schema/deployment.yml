apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-hub-schema
  labels:
    app: event-hub-schema
    tier: validator
spec:
  replicas: 2
  selector:
    matchLabels:
      app: event-hub-schema
      tier: validator
  template:
    metadata:
      labels:
        app: event-hub-schema
        tier: validator
    spec:
      containers:
        - name: event-hub-schema
          image: us-central1-docker.pkg.dev/event-hub-317019/event-hub/event-hub-schema:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
